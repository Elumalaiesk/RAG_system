# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic-settings>=2.6,<3.0
python-multipart>=0.0.20,<1.0

# --- RAG pipeline ---
pypdf>=5.1,<7.0                    # PDF text extraction (stage 1: parse)
sentence-transformers>=3.3,<6.0    # local embedding model (stage 3: embed)
chromadb>=0.5.20,<2.0              # persistent vector store (stage 4: index/retrieve)
anthropic>=1.0                     # Claude (LLM_PROVIDER=anthropic)
openai>=1.50                       # Gemini via its OpenAI-compatible endpoint

# --- Tests ---
pytest>=8.0,<9.0
httpx>=0.27,<1.0
